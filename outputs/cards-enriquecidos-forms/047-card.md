Scenario: An extractor pulls line items and an invoice total from a receipt. The strongest integrity check before accepting the output is to:

---

[ ] A - Trust the total field because it is printed prominently.B.Verify that the line items sum to the extracted total, and on a mismatch retry or flag the record.C.Check only that the total is a number.D.Accept the first extraction without checking.
[ ] B - Verify that the line items sum to the extracted total, and on a mismatch retry or flag the record.C.Check only that the total is a number.D.Accept the first extraction without checking.
[ ] C - Check only that the total is a number.D.Accept the first extraction without checking.
[ ] D - Accept the first extraction without checking.
