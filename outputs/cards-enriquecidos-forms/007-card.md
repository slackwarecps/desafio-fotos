Scenario: A README says the auth check happens in one module, but the agent must be sure before changing it. The agent should:

---

[ ] A - Trust the README and edit the module it names.B.Confirm in the current code where the auth check actually runs, then make the change there.C.Search the commit history for the original author and ask them.D.Assume the check moved and search at random.
[ ] B - Confirm in the current code where the auth check actually runs, then make the change there.C.Search the commit history for the original author and ask them.D.Assume the check moved and search at random.
[ ] C - Search the commit history for the original author and ask them.D.Assume the check moved and search at random.
[ ] D - Assume the check moved and search at random.
