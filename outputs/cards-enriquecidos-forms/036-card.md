Scenario: An agent must find why a specific error message is thrown in a large service. The most context-efficient first step is to:

---

[ ] A - Read the whole service top to bottom to build a full picture.B.Search for the exact error string, then open only the files and functions that produce or handle it.C.Open the largest file first on the assumption that is where the logic lives.D.Rewrite the error handling and see if the message changes.
[ ] B - Search for the exact error string, then open only the files and functions that produce or handle it.C.Open the largest file first on the assumption that is where the logic lives.D.Rewrite the error handling and see if the message changes.
[ ] C - Open the largest file first on the assumption that is where the logic lives.D.Rewrite the error handling and see if the message changes.
[ ] D - Rewrite the error handling and see if the message changes.
