Scenario: Before renaming a widely used function, an agent needs to know what a change would break. The right move is to:

---

[ ] A - Rename it and run the build to see what fails.B.Search the codebase for all references first, then plan the change across the call sites.C.Rename only the definition and assume callers will adapt.D.Add a second function and leave the old one untouched.
[ ] B - Search the codebase for all references first, then plan the change across the call sites.C.Rename only the definition and assume callers will adapt.D.Add a second function and leave the old one untouched.
[ ] C - Rename only the definition and assume callers will adapt.D.Add a second function and leave the old one untouched.
[ ] D - Add a second function and leave the old one untouched.
